template(name='navButtons')
  //- ul.nav.nav-pills
    //- each tribes
      //- li(role="presentation"): a(href="{{ pathFor 'tribe' _id}}")=name
  .nav-buttons
    each tribes
      a.nav-button(href="{{ pathFor 'tribe' _id}}")=name
